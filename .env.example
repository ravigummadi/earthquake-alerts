# Environment variables for earthquake-alerts
# Copy this to .env and fill in your values

# Slack webhook URLs (create at https://api.slack.com/messaging/webhooks)
SLACK_WEBHOOK_CRITICAL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_WEBHOOK_ALL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_WEBHOOK_NEARBY=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# GCP Project (optional - uses default if not set)
GCP_PROJECT=your-project-id

# Configuration path (optional)
CONFIG_PATH=config/config.yaml

# Log level (optional)
LOG_LEVEL=INFO

# Simple mode - use these instead of a config file for basic setup
# MONITORING_BOUNDS=35.9024,39.18543,-122.92603,-120.71777
# MIN_MAGNITUDE=3.0
# LOOKBACK_HOURS=1
